require "spec_helper"

describe Kaminari::Core do
  it "has a version number" do
    expect(Kaminari::Core::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
